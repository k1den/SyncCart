<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SyncCart_V20" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- Основной цвет -->
        <item name="colorPrimary">@color/brand_green</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Убирает фиолетовые эффекты -->
        <item name="colorSecondary">@color/brand_green</item>
        <item name="colorTertiary">@color/brand_green</item>

        <!-- Цвет ripple / выделений -->
        <item name="colorControlHighlight">@color/brand_green</item>

    </style>

    <style name="Theme.SyncCart_V20" parent="Base.Theme.SyncCart_V20" />
</resources>